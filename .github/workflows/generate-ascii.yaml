name: Generage ASCII Artwork
on: push
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v7
      
    - name: List Repo files
      run: ls
    
    - name: execute ascii script
      run: |
        chmod +x ascii-script.sh
        ./ascii-script.sh